/**
 * @deprecated Import from `@/lib/contentful/hub/hubService` (or submodules `delivery` / `management`).
 * Barrel kept so existing paths keep working.
 */
export * from '@/lib/contentful/hub/hubService';
